/*
let endofbucketlist = true
let bucketlist = []
while (endofbucketlist == true) {
  add_or_not = prompt("Do you want to add to your bucket list?")
  }
  if (add_or_not == "no") {
    console.log(bucketlist)
    }
else if (add_or_not == "yes"){
      adding = prompt("What item you would like to add?")
      bucketlist.push(adding)
      console.log(bucketlist)
      }
else endofbucketlist = false
      */
      
      
      
      